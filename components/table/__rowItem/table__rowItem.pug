mixin addRowItem(item)
    tr
        each val in item.fieldsData
            td
                h3=val
        td
            .favourite
            .icon_cross(data-action="remove")
+addRowItem(item)